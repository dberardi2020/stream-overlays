{
  "name": "stream-overlays",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Browser overlays for streamers, config-in-the-URL, static-first. Sim-racing (Logitech G923) is the first vertical.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server --directory overlays/sim-racing"
  }
}
